# 方案计划（2026-08-08 · 第 15 轮）

## 当前开发日：Day 28（全量测试 + 性能 · #4 域；F-31 已收口）

> **本轮性质：头部确认版 v6**（第 14 轮「F-31 正式方案」→ 第 15 轮「F-31 收口确认 + 头部刷新」）。
> 触发：**F-31（P0 用户拍板三子项）已由 #3 16:35 窗口落地收口**（git 实测见下）——Day 28 唯一客观开发任务集 D28-F31-1~3 + EXIT 全 [x]（TASKS.md:2241-2266），**无开发任务待方案化**；剩余 [ ]（TASKS:2279-2280）= #4 域（性能 + TEST_REPORT），无需开发方案。
> 方案师本岗仅覆盖写头部确认版 + TASKS 标注，红线全程遵守（不写码 / 不改 .gd/.tscn/.tres/.json / 不 commit / 不跑探针）。

---

## 0) P0 调度硬性输入检查（读 PLAYTEST 追踪区头部 · 本轮实测 17:25）

- ✅ **F-31 武器升级体系（P0 · 用户拍板 · #2 第 38 轮拆解）= 已收口，无需再方案**：
  - git 实测：`f30d402`（D28-F31-1 初始武器出商店池：data_loader 新增 `get_starting_weapon_ids()` 纯函数 + shop `_build_shop_pool` 武器循环跳过，池口径 36−3结果−10起始 = 23 武器）+ `f0606bf`（D28-F31-2/3 b+c 同批：服务池 anvil 入池池 49 + `_purchase_item` 第三分支 + `_show_anvil_panel` 动态 UI + `_apply_anvil_upgrade`；level_up_panel 删武器升级段保留属性/进化 + F-20 保底）。
  - 护栏：**day28_f31_check 26/26 CLEAN + 回归 29/29（759 断言）+ BASELINE CLEAN**（#3 自证 + #53 反馈专员轻量复核实码在位 + #54 复核一致 ✅）。
  - TASKS D28-F31-1~3/EXIT 全 [x]（:2241-2266 逐行实证），**第 14 轮方案全部执行完毕**。
- ✅ **增量 #54（17:5x · 最新）= F-31 落地确认 + 艾琳动画 v3 修正观察轮**：无新机器可验证 P0（F-31 🟢 待真人回归 = 主观项；**U-1 艾琳动画 v3 已提交 `57adaea`**（用户 17:2x 目视反馈「实现的不是很好」→ 根因 v2 格数误判 64×64 vs 实际 60×60 → v3 自动测格 40px + 60×60 + 统一 bbox 裁剪窗口 + 最大连通域剔装饰点，输出仍 64×64 = 探针断言零改动；验证 day21_22 38/38 + day26 34/34 全绿）= **待用户重新目视，主观项**）。
- 🔴 **Day 28 性能段（#4 域）跨第 8 轮零开工维持**：裁决态②「交 Owner 三选」未决（核查 #4 / 降级 D30 兜底 / 授权补登记口径），**不属 #3/#方案师**。
- 🔴 **P0 其余检查**：F 系列（F-01~F-31 + T-C/T-D）全 🟢「已落地 · 待真人回归」= 主观项交 #5；E-0 阶段 E 首段终审完整局（含 U-1 v3 重新目视）= 真人侧最高优先；无其他新机器可验证 P0。
- 🟠 **美术资源策略（08-07 21:1x 拍板）遵守**：本轮零美术任务（F-31 已落地零新图；艾琳 v3 为用户直派例外先例）。

---

## 1) 任务方案（TASKS Day 28 区 · 当前状态核验）

### 任务 D28-F31-1~3 + EXIT【已全 [x] 收口 · 无方案动作】

> 第 14 轮方案（覆盖写前的完整版）为 #3 16:35 窗口执行依据，已全部执行完毕（git 实证 `f30d402` + `f0606bf`，见 §0）。**本岗不再重复方案**。

### 任务（TASKS:2279-2280）【#4 域 · 无需开发方案】

- [ ] 自动化测试 + 性能（帧率/内存/同屏敌人数）——**#4 域**：性能段跨第 8 轮零开工（tools/ 实测无 perf/stress/day28 脚本），裁决态②交 Owner 未决；**请 #4 下轮（#39）补齐三项性能探针或按裁决口径处理**（结果由下一窗口 19:20 观察）。
- [ ] 回归 `baseline_check`；产出 `docs/TEST_REPORT.md`——**#4 域**：TEST_REPORT 止于 #38（16:00，快照 HEAD=`1d86a19`）**早于其后 5 提交**（`f30d402`/`f0606bf`/`337800e`/`57adaea`/`074af59`）→ **请 #4 下轮（#39）正式纳入 day28_f31_check（26 断言）= 二十九件套 ≥759 断言，快照覆盖 `074af59`**（#53/#54 请求合并确认）。

---

## 2) 风险与观察点（供 #4/#5/#1 参考，非 #3 执行输入）

| # | 观察点 | 归属 | 状态 |
|---|--------|------|------|
| 1 | TEST_REPORT #38 快照滞后（1d86a19 → 需覆盖 074af59，+5 提交含 F-31 双提交） | #4 #39 | ⚠️ 请下轮修复 |
| 2 | day28_f31_check（26 断言）未入 runner | #4 #39 | ⚠️ 请下轮并入 → 29 件套 759 |
| 3 | 性能段（帧率/内存/同屏敌人数）零开工跨第 8 轮 | #4/Owner | 🔴 裁决态②未决 |
| 4 | U-1 艾琳动画 v3 已提交（`57adaea`） | 真人/用户 | 🟡 待重新目视 |
| 5 | F-31 真人回归面 3 子项（商店不再刷起始武器 / 升级面板纯属性+进化 / 铁砧 120G 闭环） | #5 真人 | 🟢 待真人回归 |
| 6 | R4 攻击力口径挂账 | Owner | ⏳ 第 30 轮维持 |

---

## 3) 展望（后续窗口）

- **Day 28**：F-31 已收口 → 剩余全为 #4 域（#39 覆盖 + 性能段裁决）；方案师维持头部确认轮。
- **Day 29 人工试玩 + 修复** = 真人域（#5 收真人反馈 + #3 按需修复）——待 #2 拆解/真人反馈后若产生开发任务，方案师再定案。
- **Day 30 发布准备** = #3/#1 域（build_release.py / Steam 构建 / 资产上传）——若 #1 裁决需 #3 参与，方案师再写正式方案；当前 #2 已转维护模式零拆解。
- **红线遵守**：不写码 / 不改 .gd/.tscn/.tres/.json / 不 commit / 不跑探针；产出仅 SOLUTION_PLAN.md + TASKS 标注 + 记忆文件。
